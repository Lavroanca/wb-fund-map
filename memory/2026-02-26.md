# 2026-02-26

- Лавр перенёс OpenClaw на более мощный сервер (2 vCPU, ~4 ГБ RAM), чтобы я работал быстрее и не упирался в железо.
- Конфиг OpenClaw настроен на OpenAI Codex OAuth (`openai-codex/gpt-5.1`), без прямых API-ключей; Лавр осознанно предпочитает подписку ChatGPT Plus/Pro вместо прямого API после того, как я ранее сжёг около $15 за день через API.
- Мы обсудили расходы токенов и договорились, что разумно включить экономные настройки (heartbeat, pruning, ограничение bootstrap), чтобы снизить нагрузку на подписку.
- Всплыл старый доступ к Google Sheets: раньше использовался API-ключ и сервисный аккаунт `volfram@analiti-483916.iam.gserviceaccount.com`; этот доступ считается потенциально скомпрометированным, и в будущем нужно создавать новые ключи и хранить их только локально на сервере.
- Лавр хочет автоматизировать анализ недвижимости: есть карта фондимущества с лотами (мы вытащили JSON-ответы с `latitude`/`longitude`) и карта зон поддержки Wildberries; цель — сделать пайплайн, который будет автоматически находить помещения с торгов, попадающие в зоны поддержки WB.

- Лавр начал разбираться с картой WB: сохранил HAR с запросами `map.wb.ru` / `wb-maps.wb.ru` / `pvz-map-backend.wildberries.ru`. Я ему подсказал, что HAR — это ок как черновик, но для нашей задачи достаточно вытащить конкретные ссылки/файлы тайлов (`1190.pbf` / `.mvt`) или сами файлы и залить их мне в воркспейс.
- Следующий шаг по WB: через DevTools → Network → Filter по `1190` → правой кнопкой по `1190.pbf` → `Copy link address` или `Open in new tab` и прислать либо ссылку, либо файл. После этого я смогу написать скрипт, который декодирует векторный тайл в GeoJSON-полигоны для анализа «лот ∈ зона WB».
